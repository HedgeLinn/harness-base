[project]
name = "code-assistant-backend"
version = "0.1.0"
description = "代码仓库助手场景的内核逻辑层（FastAPI + agent 执行链）"
requires-python = ">=3.9"

[project.optional-dependencies]
dev = ["pytest>=7.0", "fastapi>=0.110", "uvicorn>=0.29"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
