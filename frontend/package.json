{
  "name": "harness-base-frontend",
  "version": "0.2.0",
  "private": true,
  "description": "通用 harness 底座的前端界面层（Node/TypeScript）",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.build.json"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
